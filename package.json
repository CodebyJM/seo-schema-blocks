{
	"name": "seo-schema-blocks",
	"version": "0.1.0",
	"private": true,
	"description": "Gutenberg blocks for JSON-LD schema (Local Business, etc.).",
	"scripts": {
		"build": "wp-scripts build --output-path=build",
		"start": "wp-scripts start --output-path=build"
	},
	"devDependencies": {
		"@wordpress/scripts": "^27.9.0"
	},
	"browserslist": [
		">0.2%",
		"not dead",
		"not op_mini all"
	]
}
